# Vocabulary versions implemented in this repo.
# Compare against spec/VOCAB_VERSIONS to detect drift.
# Run scripts/check-downstream-versions.sh from spec/ to check all repos at once.
# Update this file when adding new model/predicate support for a vocabulary version.
core=3.3
health=2.4
clinical=1.9
coverage=1.3
# coverage=1.3: ClaimRecord, BenefitStatement, DenialNotice, AppealRecord added
# core=3.0: AIExtractionActivity, AIDiscardedExtraction, SocialHistoryConsent dataclasses added
# core=3.1-3.3: AdvisoryApplicationActivity, AIGenerationActivity, GenerationTrigger, ProxyAgent, AIAsserted provenance leaf added
# clinical=1.8: ClinicalSocialHistoryRecord dataclass added (EHR-extracted, 42 CFR Part 2)
# clinical=1.9: cascade:AIExtracted permitted as a dataProvenance value on clinical records
# health=2.4: SocialHistoryRecord dataclass added (consumer-reported lifestyle data)
checkup=3.2
pots=1.4
# clinical=1.8: Medication RDF type and predicates migrated from health: to clinical: namespace
#
# 2026-07-15 batched draft sync (spec PENDING_DOWNSTREAM_SYNC rows 1-3). Per
# D-PATH, pre-stable drafts stay UNROWED until v1.0 graduation; the namespaces +
# predicates were registered in vocabularies/namespaces.py without a version row:
#   - evidence@v1-draft.0.2: verdict-taxonomy-v2 facet predicates (direction,
#     basis, strength, settled, reason, confidence) + the evidence namespace.
#   - workbench@v1-draft.0.4: workbench:userSourceLabel + the workbench namespace.
#   - workbench@v1-draft.0.5 notes substrate: external namespaces oa, ical, skos
#     registered so oa:Annotation notes (incl. the workbench:followUp motivation
#     individual) round-trip. No JSON-LD context for drafts yet.
